# 部署層環境變數範本。複製成專案【根目錄】的 .env（不進版控）。
# 給 docker-compose 讀（對外 port + 命名 + labels）；與後端應用層 backend/.env 分開。
# 說明見 reference/deploy/deploy-guide.md §3。

# ── image / 網路命名（compose 與 build.sh 都吃同一組）──
IMAGE_PREFIX=multimodel
IMAGE_TAG=0.1
NETWORK_NAME=multimodel-network

# ── 路由名稱（內外網共用；前端掛在 /<APP_ROUTE>/）──
# 內網 http://<ip>:<FRONTEND_PORT>/<APP_ROUTE>/、外網 https://<DNS>/<APP_ROUTE>/
# 不含前後斜線。改動後前端 image 需重新 build（VITE_BASE_PATH 由它帶入）。
APP_ROUTE=multimodel

# ── 對外 port（依實際情況改）── ★ FRONTEND_PORT 請自行設定成你要的對外埠 ★
FRONTEND_PORT=8080
BACKEND_PORT=8000          # 僅開發期在 compose 打開 backend.ports 時用到

# ── labels（容器歸屬盤點）──
DEVELOPER_NAME=aidan
PROJECT_PATH=/home/aidankao/aidan/study/claude-multimodel
